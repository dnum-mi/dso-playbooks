- name: Provisionning DSO
  hosts: localhost
  tasks:
    - name: Disclaimer
      ansible.builtin.debug:
        msg: "Attention ! La suppression d'un projet ne supprimes pas les applications Argo, utilisez d'abord le playbook 'delete_git_project.yml' pour les effacer"
    - include_role:
        name: "{{ item }}"
      loop:
        - ldap-delete-project
        # - sonar-provisioning-user TODO
        - keycloak-remove-group-user
        - keycloak-delete-project
        - quay-delete-setup
        - gitlab-delete-group
        - gitlab-remove-group-user # à vérifier
        - delete-namespace-openshift
        - argo-delete-project
        # - nexus-provision-project TODO
